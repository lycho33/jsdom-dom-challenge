function timer(timeP) {
    var d = new Date();
    var sec = d.getSeconds();
    lblJsTimer.SetText(sec);
}

function timer(sec) {
    let c = document.getElementById("counter")
    
    document.write(sec)
}
